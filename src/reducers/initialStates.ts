import { initialState as bookInitialState } from './book';
import { initialState as themeInitialState } from './theme';

// eslint-disable-next-line import/no-unused-modules
export default {
  book: bookInitialState,
  theme: themeInitialState,
};
